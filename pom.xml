<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.floci.az</groupId>
    <artifactId>floci-az</artifactId>
    <version>0.12.0</version>

    <properties>
        <compiler-plugin.version>3.16.0</compiler-plugin.version>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.39.2</quarkus.platform.version>
        <!-- Coupled to the Artemis sidecar image (application.yml *.artemis-image): the patched
             artemis-amqp-protocol and proton-j jars built below replace the same-version jars inside
             that image, so these two only move together with the image tag. Guarded by
             ArtemisPatchVersionsTest; excluded from the dependabot group in .github/dependabot.yml. -->
        <artemis.version>2.44.0</artemis.version>
        <proton-j.version>0.34.1</proton-j.version>
        <skipITs>true</skipITs>
        <surefire-plugin.version>3.6.0</surefire-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-config-yaml</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
          <!-- brotli4j resolves a JNI jar for the build host's platform (native-osx-aarch64 on a
               Mac, native-linux-* in CI); floci-az never enables Brotli HTTP compression. Maven
               wildcards match a whole artifactId only, hence the explicit list. -->
          <exclusions>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-osx-aarch64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-osx-x86_64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-linux-aarch64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-linux-x86_64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-windows-x86_64</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-xml</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-cbor</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>

        <!-- Docker client for Azure Functions container execution -->
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-api</artifactId>
            <version>3.7.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-transport</artifactId>
            <version>3.7.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-core</artifactId>
            <version>3.7.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-transport-httpclient5</artifactId>
            <version>3.7.1</version>
        </dependency>
        <dependency>
            <groupId>org.brotli</groupId>
            <artifactId>dec</artifactId>
            <version>0.1.2</version>
        </dependency>
        <!-- TAR streaming for code injection into containers -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-compress</artifactId>
            <version>1.28.0</version>
        </dependency>
        <!-- GraalVM substitution annotations (see core/docker/graal); provided, never packaged. -->
        <dependency>
            <groupId>org.graalvm.sdk</groupId>
            <artifactId>nativeimage</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- BouncyCastle for self-signed TLS cert generation (Artemis AMQP TLS) -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.85.2</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.85</version>
        </dependency>
        <!-- proton-j for Service Bus CBS responder (same version as azure-core-amqp) -->
        <dependency>
            <groupId>org.apache.qpid</groupId>
            <artifactId>proton-j</artifactId>
            <version>${proton-j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.activemq</groupId>
            <artifactId>artemis-amqp-protocol</artifactId>
            <version>${artemis.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5-mockito</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                            <goal>native-image-agent</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
                <executions>
                    <execution>
                        <id>unpack-servicebus-proton-j</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.qpid</groupId>
                                    <artifactId>proton-j</artifactId>
                                    <version>${proton-j.version}</version>
                                    <outputDirectory>${project.build.directory}/artemis-proton-classes</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unpack-servicebus-artemis-amqp</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.activemq</groupId>
                                    <artifactId>artemis-amqp-protocol</artifactId>
                                    <version>${artemis.version}</version>
                                    <outputDirectory>${project.build.directory}/artemis-amqp-classes</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unpack-servicebus-artemis-amqp-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <overWriteReleases>true</overWriteReleases>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.activemq</groupId>
                                    <artifactId>artemis-amqp-protocol</artifactId>
                                    <version>${artemis.version}</version>
                                    <classifier>sources</classifier>
                                    <outputDirectory>${project.build.directory}/artemis-amqp-patch-sources</outputDirectory>
                                    <includes>
                                        org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java,
                                        org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java,
                                        org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java,
                                        org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java,
                                        org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java,
                                        org/apache/activemq/artemis/protocol/amqp/proton/AMQPMessageWriter.java,
                                        org/apache/activemq/artemis/protocol/amqp/proton/AMQPLargeMessageWriter.java
                                    </includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>patch-servicebus-artemis-amqp-sources</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java"
                                    token="deliveryCount &gt; 1"
                                    value="deliveryCount &gt; 0"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java"
                                    token="final int amqpDeliveryCount = deliveryCount - 1;"
                                    value="final int amqpDeliveryCount = deliveryCount;"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java"
                                    token="AMQPMessageSupport.SCHEDULED_DELIVERY_TIME, AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY};"
                                    value="AMQPMessageSupport.SCHEDULED_DELIVERY_TIME, AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY, Symbol.valueOf(&quot;x-opt-scheduled-enqueue-time&quot;)};"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java"
                                    token="AMQPMessageSymbolSearch.kmpNeedleOf(AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY)};"
                                    value="AMQPMessageSymbolSearch.kmpNeedleOf(AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY), AMQPMessageSymbolSearch.kmpNeedleOf(Symbol.valueOf(&quot;x-opt-scheduled-enqueue-time&quot;))};"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java"
                                    token="Object objdelay = getMessageAnnotation(AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY);"
                                    value="Object objdelay = getMessageAnnotation(AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY); Object objAzureScheduledTime = getMessageAnnotation(Symbol.valueOf(&quot;x-opt-scheduled-enqueue-time&quot;));"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java"
                                    token="if (objscheduledTime != null &amp;&amp; objscheduledTime instanceof Number number) {"
                                    value="if (objAzureScheduledTime instanceof java.util.Date date) { this.scheduledTime = date.getTime(); } else if (objAzureScheduledTime instanceof Number number) { this.scheduledTime = number.longValue(); } else if (objscheduledTime != null &amp;&amp; objscheduledTime instanceof Number number) {"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="selector = filter.getValue().getDescribed().toString();"
                                    value="selector = EventHubFilterSupport.rewriteSelector(filter.getValue().getDescribed().toString());"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="protonSender.setReceiverSettleMode(ReceiverSettleMode.FIRST);"
                                    value="protonSender.setReceiverSettleMode(protonSender.getRemoteReceiverSettleMode());"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="final String sourceAddress = ParameterisedAddress.extractAddress(source.getAddress());"
                                    value="final String sourceAddress = ServiceBusSessionSupport.normalizeEntityPath(AmqpEntityAddress.toEntityPath(ParameterisedAddress.extractAddress(source.getAddress())));"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="source.setFilter(supportedFilters.isEmpty() ? null : supportedFilters);"
                                    value="selector = ServiceBusSessionSupport.configure(sessionSPI.getProtocolManager().getServer(), queue, source, supportedFilters, protonSender, senderContext, selector); source.setFilter(supportedFilters.isEmpty() ? null : supportedFilters);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="public void close(boolean remoteClose) throws Exception {"
                                    value="public void close(boolean remoteClose) throws Exception { ServiceBusSessionSupport.release(protonSender);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="addressToUse = SimpleString.of(sourceAddress);"
                                    value="addressToUse = SimpleString.of(sourceAddress);&#10;            directQueue = sessionSPI.queueQuery(QueueConfiguration.of(addressToUse), false);&#10;            if (directQueue.isExists()) {&#10;               queueNameToUse = addressToUse;&#10;               addressToUse = directQueue.getAddress();&#10;            }"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="final boolean isFQQN;"
                                    value="final boolean isFQQN;&#10;         QueueQueryResult directQueue = null;"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/DefaultSenderController.java"
                                    token="multicast = verifySourceCapability(source, TOPIC_CAPABILITY);"
                                    value="multicast = directQueue != null &amp;&amp; directQueue.isExists() ? directQueue.getRoutingType() == RoutingType.MULTICAST : verifySourceCapability(source, TOPIC_CAPABILITY);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java"
                                    token="address = SimpleString.of(targetAddress);"
                                    value="address = SimpleString.of(AmqpEntityAddress.toEntityPath(targetAddress));"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java"
                                    token="import org.apache.activemq.artemis.protocol.amqp.broker.AMQPSessionCallback;"
                                    value="import org.apache.activemq.artemis.protocol.amqp.broker.AMQPMessage;&#10;import org.apache.activemq.artemis.protocol.amqp.broker.AMQPSessionCallback;&#10;import org.apache.activemq.artemis.protocol.amqp.broker.ServiceBusBatchSupport;"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java"
                                    token="sessionSPI.serverSend(this, tx, receiver, delivery, address, deduceRoutingType(message), routingContext, message);"
                                    value="if (message instanceof AMQPMessage amqpMessage &amp;&amp; ServiceBusBatchSupport.isBatch(amqpMessage)) {&#10;            for (Message batchMessage : ServiceBusBatchSupport.decode(amqpMessage, sessionSPI.getCoreMessageObjectPools())) {&#10;               sessionSPI.serverSend(this, tx, receiver, delivery, address, deduceRoutingType(batchMessage), routingContext, batchMessage);&#10;            }&#10;         } else {&#10;            sessionSPI.serverSend(this, tx, receiver, delivery, address, deduceRoutingType(message), routingContext, message);&#10;         }"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="doAck(message);"
                                    value="doAck(message); delivery.disposition(Accepted.getInstance());"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="import org.apache.qpid.proton.amqp.messaging.Modified;"
                                    value="import org.apache.qpid.proton.amqp.messaging.Modified;&#10;import org.apache.qpid.proton.amqp.messaging.Rejected;"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="if (settleImmediate) {"
                                    value="if (settleImmediate) { delivery.disposition(remoteState instanceof Rejected ? new Rejected() : remoteState);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="if (Boolean.TRUE.equals(modification.getDeliveryFailed())) {"
                                    value="if (!Boolean.TRUE.equals(modification.getUndeliverableHere())) {"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="sessionSPI.reject(brokerConsumer, message);"
                                    value="ServiceBusDeadLetterSupport.apply(message, (Rejected) remoteState); sessionSPI.reject(brokerConsumer, message);"
                                    failOnNoReplacements="true" />
                                <!-- Broker ingress timestamps and dead-letter properties can be newer than
                                     the large-message file. Copy the current metadata before streaming its body. -->
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java"
                                    token="if (isDLQOrExpiry) {"
                                    value="if (isDLQOrExpiry || getIngressTimestamp() != null) {"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java"
                                    token="bufferNewHeader = newHeaderWithoutExpiry(place);"
                                    value="bufferNewHeader = getIngressTimestamp() == null ? newHeaderWithoutExpiry(place) : ServiceBusLargeMessageSupport.headerForCopy(this, place, isDLQOrExpiry);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="private boolean closed = false;"
                                    value="private boolean closed = false;&#10;   private final ServiceBusMessageLockSupport messageLocks;"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="this.sessionSPI = server;"
                                    value="this.sessionSPI = server;&#10;      this.messageLocks = new ServiceBusMessageLockSupport(connection, server);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="delivery.setMessageFormat(messageFormat);"
                                    value="delivery.setMessageFormat(messageFormat);&#10;      messageLocks.track(delivery, messageReference, brokerConsumer, preSettle);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="final MessageReference reference = (MessageReference) delivery.getContext();"
                                    value="if (!messageLocks.beforeSettlement(delivery)) { return; }&#10;         final MessageReference reference = (MessageReference) delivery.getContext();"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java"
                                    token="messageWriter.close();"
                                    value="messageLocks.close(); messageWriter.close();"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/AMQPMessageWriter.java"
                                    token="final ReadableBuffer sendBuffer = amqpMessage.getSendBuffer(messageReference.getDeliveryCount(), messageReference);"
                                    value="final ReadableBuffer sendBuffer = ServiceBusMessageMetadataSupport.forDelivery(amqpMessage, messageReference).getSendBuffer(messageReference.getDeliveryCount(), messageReference);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/AMQPLargeMessageWriter.java"
                                    token="if (message.isReencoded()) {"
                                    value="if (message.isReencoded() || ServiceBusMessageMetadataSupport.isServiceBus(reference)) {"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/AMQPLargeMessageWriter.java"
                                    token="MessageAnnotations messageAnnotations = AMQPMessageBrokerAccessor.getDecodedMessageAnnotations(message);"
                                    value="MessageAnnotations messageAnnotations = ServiceBusMessageMetadataSupport.annotationsForDelivery(AMQPMessageBrokerAccessor.getDecodedMessageAnnotations(message), message, reference);"
                                    failOnNoReplacements="true" />
                                <replace
                                    file="${project.build.directory}/artemis-amqp-patch-sources/org/apache/activemq/artemis/protocol/amqp/proton/AMQPLargeMessageWriter.java"
                                    token="Header header = AMQPMessageBrokerAccessor.getCurrentHeader(message);"
                                    value="Header header = ServiceBusMessageLockSupport.headerForDelivery(AMQPMessageBrokerAccessor.getCurrentHeader(message), reference);"
                                    failOnNoReplacements="true" />
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>package-servicebus-artemis-patches</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <copy todir="${project.build.directory}/artemis-plugin-classes">
                                    <fileset dir="${project.basedir}/src/artemis-plugin/resources" />
                                </copy>
                                <mkdir dir="${project.build.directory}/classes/artemis" />
                                <!-- Single source of truth for the patch jar names and the sidecar lib
                                     paths: the namespace managers read this file instead of hardcoding
                                     the versions. -->
                                <propertyfile file="${project.build.directory}/classes/artemis/patch-versions.properties">
                                    <entry key="artemis.version" value="${artemis.version}" />
                                    <entry key="proton-j.version" value="${proton-j.version}" />
                                </propertyfile>
                                <jar
                                    destfile="${project.build.directory}/classes/artemis/servicebus-artemis-extension.jar"
                                    basedir="${project.build.directory}/artemis-plugin-classes" />
                                <jar
                                    destfile="${project.build.directory}/classes/artemis/proton-j-${proton-j.version}-floci-az-proton-patch.jar"
                                    basedir="${project.build.directory}/artemis-proton-classes" />
                                <jar
                                    destfile="${project.build.directory}/classes/artemis/artemis-amqp-protocol-${artemis.version}-floci-az-artemis-amqp-patch.jar"
                                    basedir="${project.build.directory}/artemis-amqp-classes" />
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
                <executions>
                    <execution>
                        <id>compile-servicebus-artemis-extension</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <release>21</release>
                            <compileSourceRoots>
                                <compileSourceRoot>${project.basedir}/src/artemis-plugin/java</compileSourceRoot>
                            </compileSourceRoots>
                            <outputDirectory>${project.build.directory}/artemis-plugin-classes</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>compile-servicebus-proton-patch</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <release>21</release>
                            <compileSourceRoots>
                                <compileSourceRoot>${project.basedir}/src/artemis-patch/java</compileSourceRoot>
                            </compileSourceRoots>
                            <outputDirectory>${project.build.directory}/artemis-proton-classes</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>compile-servicebus-artemis-amqp-patch</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <release>21</release>
                            <compileSourceRoots>
                                <compileSourceRoot>${project.basedir}/src/artemis-amqp-patch/java</compileSourceRoot>
                                <compileSourceRoot>${project.build.directory}/artemis-amqp-patch-sources</compileSourceRoot>
                            </compileSourceRoots>
                            <outputDirectory>${project.build.directory}/artemis-amqp-classes</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <systemPropertyVariables>
                        <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <skipITs>false</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
            </properties>
        </profile>
    </profiles>
</project>
